name: spoomn_core
description: Shared models, constants, and DTOs for Spoomn.
version: 0.1.0
publish_to: none

environment:
  sdk: "^3.8.0"

dependencies:
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.9
  freezed: ^3.2.5
  json_serializable: ^6.8.0
  test: ^1.25.0
